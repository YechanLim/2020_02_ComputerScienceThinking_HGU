#include <stdio.h>
#include <stdlib.h>


int main() {
	int a;
	scanf("%d",&a);
	   int* b_list[4];
b_list[0] = malloc(sizeof(int)*a);
b_list[1] = malloc(sizeof(int)*a);
b_list[2] = malloc(sizeof(int)*a);
b_list[3] = malloc(sizeof(int)*a);

printf("done");

}
